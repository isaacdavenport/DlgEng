<Window x:Class="DialogEngine.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DialogEngine"
             WindowStartupLocation="CenterScreen"
             mc:Ignorable="d" 
             d:DesignHeight="451.32" Title="DialogEngine" Width="639.305"
             Loaded="Window_Loaded">
    <Window.Resources>
        <Style x:Key="MultiLineTB" TargetType="TextBox">
            <Setter Property="Text" Value="{Binding Text, ElementName=tbMaster}"/>
        </Style>
    </Window.Resources>
    <Grid Margin="0,0,2,39">
        <Grid.RowDefinitions>
            <RowDefinition Height="51*"/>
            <RowDefinition Height="183*"/>
            <RowDefinition Height="46*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="157*"/>
            <ColumnDefinition Width="360*"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="TestOutput" Height="320" TextWrapping="Wrap" Text="" VerticalAlignment="Top" Grid.ColumnSpan="2" Grid.RowSpan="2" 
                 ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="True" Style="{StaticResource MultiLineTB}" AcceptsReturn="True" Margin="0,0,-0.333,0"/>
        <TextBox x:Name="TestInput" HorizontalAlignment="Left" Height="63" Margin="0,0,-0.333,-0.333" Grid.Row="2" TextWrapping="Wrap" VerticalAlignment="Top" Width="631" Grid.ColumnSpan="2" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"
         ScrollViewer.CanContentScroll="True" />
        <Button x:Name="InputButton" Content="Retrieve" HorizontalAlignment="Left" Margin="0,63,-0.333,-41.333" VerticalAlignment="Top" Width="631" Height="41" Grid.Row="2" Click="InputButton_Click" Grid.ColumnSpan="2"/>

    </Grid>
</Window>
